"""Validators, generators and the site builder that operate on an instance.

These read the framework's own data through ``keel_lib.KEEL`` and the
instance's content through ``keel_lib.REPO``, which are resolved separately —
see ``keel_lib`` for why.
"""
